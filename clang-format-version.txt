# Required clang-format version for this workspace.
#
# clang-format output is deterministic for a given version and .clang-format,
# and identical across macOS, Linux, and FreeBSD — but it can change between
# LLVM releases. A workspace where machines run different versions reformats
# tracked sources back and forth on every commit.
#
# One non-comment line, either form:
#   >=14   minimum major version (any newer release is accepted)
#   18     exact major version (anything else is rejected)
#
# Pinned to 22: releases 13 through 21 and release 22 disagree about compound
# literal alignment under AlignAfterOpenBracket, so a mixed workspace
# reformats tracked sources back and forth. Every 22.x tested (22.1.0,
# 22.1.5, 22.1.8) produces byte-identical output, so the major alone is
# enough; FreeBSD's quarterly 22.1.7 is accepted.
22
