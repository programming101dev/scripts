{
  "schema": "p101-wrapper-conformance-contract-v3",
  "public_api": {
    "require_unit_invocation": true,
    "require_balanced_runtime_trace": true,
    "require_fault_test_when_instrumented": true,
    "require_isolated_native_smoke_when_fallible": true,
    "require_direct_fault_outcome_receipt": true,
    "require_declared_resource_instrumentation": true
  },
  "logging": {
    "arguments_required": [],
    "results_required": []
  },
  "lifecycle_contract": "wrapper-lifecycle-contract.json",
  "platform_fault_contract": "wrapper-platform-faults.json",
  "blind_spots": [
    "Third-party and direct libc calls do not emit p101 events.",
    "Argument and result text is required only for wrappers explicitly listed by this contract.",
    "An isolated native smoke proves that the wrapper reached and returned from its native path without crashing or blocking; it does not prove every native success result or side effect.",
    "Native and wrapper behavior is compared only by lifecycle scenarios with deterministic fixtures."
  ]
}
